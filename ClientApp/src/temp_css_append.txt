
@media (max-width: 768px) {
  .achievements-wrapper, 
  .skills-wrapper,
  .experience-grid {
      grid-template-columns: 1fr !important; /* Stack cards */
  }

  .achievement-card,
  .exp-card {
      /* Disable tilt visually or just styling changes if needed */
  }
  
  /* Hide System Log and HUD on mobile to save space */
  .system-log, 
  .game-hud {
      display: none !important;
  }
  
  .section-title {
      font-size: clamp(2rem, 10vw, 3rem); /* Smaller on mobile */
      margin-bottom: 2rem;
  }

  .experience-section,
  .skills-section,
  .achievements-section {
      padding: 3rem 1rem; /* Reduced padding */
  }

  .experience-container {
      margin-left: 0;
      padding-left: 1rem;
      border-left-width: 1px;
      width: auto; /* Prevent overflow */
  }
}
